# Operational Guardrails Handoff — 2026-06-28

Picks up from [2026-06-26 handoff](handoff-2026-06-26.md) (crewai retry-loop thread).
That handoff flagged an unbounded `retrying_development` loop; this session generalized
the problem across backends, found *why* the loop never makes progress, and added the
operational guardrails (timeout / spend / recursion / salvage) that bound and recover it.

Branch: `guardrails/crewai-budget-and-tests` (off `main`, which already has the merged
G1–G7 guardrail commits). No pushes — commit-only per repo convention.

---

## TL;DR

The pipeline's failures on the LangGraph backend are **agentic, not code** failures: the
open-weights dev/QA model (deepseek-v3 via OpenRouter) behaves like a **chatbot, not an
autonomous agent** — it writes code as prose and asks "shall I save this?" instead of
calling the `file_writer` tool. The orchestration then loops: no file on disk → QA fails
→ retry → same prose → repeat, until retries exhaust. The work this session was to make
that failure **bounded, cheap, observable, and partially self-healing**, and to give all
three backends the same spend/loop protection.

Result: runs that used to hang for 20+ min or burn money in a tight loop now abort in
seconds with a clear reason, salvage what the model wrote, and fail clean.

**Afternoon addendum (multi-backend smoke compare):** LangGraph and Claude SDK passed
smoke-test eval; CrewAI still red after several fix rounds. Latest blocker was orchestrated
pytest discovering **652 tests under `evals/`** (used `Path.cwd()` = repo root, not project
workspace) → false `quality_gate_passed=False` → `retry_development` → eval **1200s timeout**.
Fix: workspace-scoped `run_pytest_discover_workspace()`. See
[Smoke-test eval session](#smoke-test-eval-session--multi-backend-compare-2026-06-28-afternoon).

---

## The core agentic problem: prose instead of tool-calls

This is the through-line for almost everything below. Worth understanding deeply.

### What the model does

The LangGraph subgraphs use ReAct agents (`create_react_agent`) given a `file_writer`
tool and instructions like *"Call file_writer — do not output test code as plain text."*
deepseek-v3 **ignores this** and instead emits, as ordinary assistant text:

> "Here are the files:
> ### 1. `main.py` (Flask Application)
> ```python
> from flask import Flask
> ...
> ```
> Would you like me to proceed with saving these files?"

It produces **correct code** — but as markdown prose, framed as a *proposal awaiting
approval*, never as a tool call. There is no human to approve, so nothing is written.
Observed at every phase: planning supervisor, development supervisor, QA engineer.

### Why this breaks the pipeline (the loop)

The phases coordinate through **files on disk**, not the chat transcript (per CLAUDE.md:
file-based handoff). So when dev writes `main.py` as prose:

1. Development "completes" — but workspace has no app module.
2. Testing runs the real quality gate (`pytest`) → `ImportError: no module named main`
   or `exit 5: no tests collected`.
3. `route_after_testing` sees the failure → routes to `retry_development`.
4. Development re-runs, emits the same prose again, still no tool call.
5. Loop until `max_retries` (3) → escalate / fail.

Each turn of the loop is a **full dev+testing phase** = several LLM calls on a slow model.
Pre-guardrail, this looked like a 5–20 min hang that produced nothing.

### Secondary agentic quirks (same model, OpenRouter)

- **Malformed tool-call args.** When deepseek *does* attempt a tool call, it sometimes
  serializes `function.arguments` as a parsed dict instead of a JSON string, or emits
  truncated/prose-wrapped JSON. This crashed the subgraph mid-run with a `JSONDecodeError`
  ("line 525 column 1") — a hard failure masquerading as a content problem.
- **Empty LLM responses.** OpenRouter occasionally returns `None`/empty for a completion
  (seen with crewai last session as `Invalid response from LLM - None or empty`). Looks
  identical to a real failure; must be retryable, not fatal.
- **Self-narration без action.** The QA agent literally logs *"I will now proceed to write
  the test files… Let me first inspect the source files. (;"* and then stops — a planning
  monologue with no tool invocation.

### Why this is *not* just "use a better model"

True — Claude models call tools reliably and the Claude Agent SDK backend doesn't show
this. But the orchestration must be **robust to non-compliant models** because:
- The whole point of the project is multi-backend comparison incl. open models.
- Even strong models occasionally narrate instead of acting.
- A pipeline that silently loops/burns money on bad model behavior is unsafe regardless of
  which model is default.

So the fixes are defensive guardrails, not a model swap.

---

## Solutions shipped (G1–G7 + crewai budget)

Ordered by how directly they address the agentic failure.

### Salvage: recover prose-as-files (G3, G6, G7) — the heart of it

If the model writes code as fenced markdown with a filename, **extract it and write it to
disk ourselves**, so the loop can make progress despite the model never calling
`file_writer`.

- **G3** — added salvage to the *testing* node + a markdown-header regex
  (``### `test_calc.py` ``). Rescued Scenario 1 (smoke test), which now passes.
- **G6** — widened the regex: the dev supervisor uses *numbered, suffixed* headers like
  ``### 1. `main.py` (Flask Application)`` and ``### 2. Updated `tests/test_api.py` `` that
  the strict pattern missed (filename not immediately after the `#`s). New
  `_MD_HEADER_LOOSE_RE` matches a **backtick-wrapped** filename anywhere in a header line
  (backticks required, to avoid capturing prose that merely mentions a file).
- **G7** — the dev-phase salvage was gated on `if not generated` (workspace empty), so once
  *any* file existed (a test from a prior retry, a small dev write) it **skipped** —
  silently dropping the prose-written `main.py`. This was the actual loop driver. Now the
  dev node **always** runs extraction over its message delta; the extractor is idempotent
  and overwrites with the latest prose (a retry's corrected version wins).

Effect: across iterations, the todo-app run went from "1 test file, nothing else" to
landing `todoapp/app.py`, `requirements.txt`, packaged tests — the app code now reaches
disk. (See "Open problem" — it still doesn't fully pass within retries.)

> **Security note baked into salvage:** the extractor validates filenames *before* writing.
> A pre-existing `lstrip("./")` was stripping arbitrary leading dot/slash chars, turning
> `../escape.py` into `escape.py` and defeating the traversal check — a real path-traversal
> hole, now fixed (strip at most one `./`, reject absolute paths, `..` segments, dotfiles).
> Adversarial tests cover it. **Do not reintroduce `lstrip("./")`.**

### Recoverable routing + error reset (G2)

A *testing-phase* error (prose-not-tool-call crash, malformed tool-call) used to route
straight to terminal `error` — one bad model turn killed the whole run. Now
`route_after_testing` treats a testing-phase error as **retryable** (back to development up
to `max_retries`, then escalate to human). Errors from other phases stay terminal.

This exposed a latent bug: the `errors` state field used `operator.add`, so nodes returning
`"errors": []` to signal "recovered" were a **no-op append** — stale errors persisted and
forced terminal routing even after recovery. New `reset_or_extend_errors` reducer treats an
empty update as a reset. `retry_development` now clears errors each attempt.

Also: `pytest` exit 5 ("no tests collected") is surfaced as a distinct
`no_tests_collected` signal with actionable reason text, so a retry can tell the agent
"you wrote no tests" rather than treating it like an ordinary test failure.

### Bounding cost and loops (G4, crewai budget)

The retry loop above is *bounded by count* (`max_retries`, `MAX_SUBGRAPH_GUARDRAIL_RETRIES`,
recursion limit) but nothing bounded **spend**. A crash/retry loop on a slow model burns
real dollars.

- **G4 (LangGraph)** — a per-run spend ceiling fed by the OpenRouter `usage.cost` in the
  httpx response hook; explicit `recursion_limit=50` instead of the implicit 25.
- **crewai budget (this session)** — CrewAI had only an *error-count* budget
  (`MAX_RUN_ERRORS=50`), not a dollar ceiling. Added a LiteLLM success callback feeding the
  same shared guard (`ai_team.core.spend_guard`), so all three backends now enforce a
  dollar ceiling (Claude SDK already had `max_budget_usd`).

**Key design — non-retryable by construction:** `BudgetExceededError` subclasses
**`BaseException`**, not `Exception`. This is deliberate and load-bearing:
- LangGraph phase nodes wrap `sub.invoke` in `except Exception` and convert failures into
  *retryable* error dicts. A budget abort must not be retried (retrying is what we're
  stopping).
- LiteLLM wraps success callbacks in `except Exception` and treats failures as
  "non-blocking" (logs and continues).

By subclassing `BaseException`, the abort **bypasses both** and propagates out to fail the
run cleanly. Caught explicitly only at the backend run/stream boundary → `success=False`,
exit 1. Verified end-to-end: `AI_TEAM_RUN_BUDGET_USD=0.002` aborts at the 3rd LLM call.

### Wall-clock watchdog (G1)

`run_demo.py` had no internal timeout — a hung LLM/tool call blocked forever (saw a 22-min
silent hang, and last session a 31-min one). Added `--timeout` (default 900s) arming a
`SIGALRM` watchdog → abort with exit 124. This is the CLI-level backstop above all the
in-graph guards.

### Spend-hook coverage (G5)

The spend *tracker* was tested, but the **hook that feeds it** (`langgraph_chat.py`,
parsing OpenRouter's `usage.cost`) had zero tests. If OpenRouter's response shape drifts,
the guard would silently record \$0 and never fire — a money-safety blind spot. Added 12
tests over the real wire shapes (cost top-level / nested / missing / malformed).

---

## Backend guardrail parity (after this session)

| Guard | crewai | langgraph | claude-agent-sdk |
|---|---|---|---|
| Dollar spend ceiling | ✅ LiteLLM cb → shared guard | ✅ httpx hook → shared guard | ✅ native `max_budget_usd` |
| Loop / turn cap | ✅ `MAX_RUN_ERRORS=50`, circuit breaker | ✅ `recursion_limit=50`, retries | ✅ `max_turns=50` |
| Wall-clock timeout | ✅ `run_demo --timeout` | ✅ `run_demo --timeout` | ✅ `run_demo --timeout` |
| Prose-as-files salvage | n/a (writes via file tools) | ✅ G3/G6/G7 | n/a (reliable tool use) |

Shared guard lives at `ai_team.core.spend_guard`; old langgraph path is a re-export shim.

---

## Demo scenario status

| Scenario | Backend | Result | Note |
|---|---|---|---|
| 1 — smoke test (calc) | langgraph | ✅ PASS | 5 tests pass; G3 salvage rescued it (QA wrote prose) |
| 2 — todo app | langgraph | ❌ model-limited | App code now lands via G7 salvage, but tests don't fully pass within retries. Guards behaved correctly: bounded to 3 retries, ~\$0.03, clean `success=false`. Not a guard bug. |
| 3 — microservices | — | ⏳ not run | |
| 4 — karpathy optimizer | claude-agent-sdk | ⏳ not run | depends on Scenario 2 output |

**Scenario 2 verdict:** blocked by deepseek's tool-call non-compliance + multi-file app
complexity, not by orchestration code. Options for a green Scenario 2: (a) model override to
a tool-compliant model for the demo, (b) run it on the claude-agent-sdk backend.

---

## Open problems / next steps (agentic focus)

### 1. The dev supervisor still under-produces even when salvaged

Latest Scenario 2 run landed `app.py` + `requirements.txt` but the QA test file imports a
module path that doesn't match the app the dev wrote (`from main import app` vs
`todoapp/app.py`). The agents don't agree on a layout because **no agent owns the project
structure** — each phase independently invents paths. The salvage gets files onto disk but
can't reconcile cross-file contracts.
→ Consider a planning-phase artifact that pins the file layout, injected into dev + QA
prompts, so imports line up. This is an *agent-coordination* fix, not a regex fix.

### 2. Salvage is a band-aid for a prompting problem

We're recovering from the model not calling `file_writer`. Better upstream: a stronger
system prompt / few-shot that forces tool use, or a `tool_choice="required"`-style
constraint where the provider supports it. Salvage should remain as defense-in-depth, but
reducing prose-output at the source would cut the retry waste (and cost/latency) entirely.

### 3. Re-prompt on detected prose-not-tool-call

Today we salvage *after* the fact. A tighter loop: detect "agent emitted code blocks but
made no `file_writer` call" within the ReAct loop and **re-prompt immediately** ("you must
call file_writer"), before the phase ends. Cheaper than a full phase retry.

### 4. Latency / cost of the retry loop

Even bounded, 3 retries × multi-call phases × ~30–60s/call (deepseek) = several wasted
minutes per failed run. The salvage + re-prompt ideas above attack this; a faster default
model for demos would too.

### 5. Remaining test/coverage gaps (lower priority, not agentic)

- **Gap 4 — coverage gate:** added `fail_under = 60` to `[tool.coverage.report]` in
  pyproject (conservative floor; CI already runs `--cov`). **Not yet committed / verified**
  — full-suite coverage couldn't be measured locally (OOM under memory pressure). Verify the
  real number in CI and ratchet the floor up.
- **Gap 3 — claude-sdk untested modules:** `reasoning_log.py`, `workspace_snapshots.py`
  (file rollback — safety-relevant), `streaming.py` have **0 test references**;
  `recovery.py` is thin. Not started.

---

## Commits this session (on `main` unless noted)

```
G1 Add operational guardrails for hung runs and QA prose-output   (5dcd2d4)
G4 Add spend + recursion guardrail to abort runaway LLM loops      (2667f91)
   docs: document LangGraph operational guardrails                 (9aad458)
G5/G6 Harden spend hook coverage and prose salvage                 (b990406)
G7 Fix dev-phase prose salvage to fire when workspace is non-empty (5206c5a)
   Add per-run dollar budget to the CrewAI backend                 (d9aba73, branch)
```

(`coverage fail_under` change is staged-but-uncommitted on the branch — see Gap 4.)

## Quick reference

```bash
# Run a demo with all guardrails + a dollar budget + wall-clock cap
AI_TEAM_RUN_BUDGET_USD=3.00 uv run python scripts/run_demo.py \
  demos/02_todo_app --backend langgraph --skip-estimate --timeout 900

# Env knobs
AI_TEAM_RUN_BUDGET_USD            # per-run spend ceiling (USD); 0 disables. default 5
AI_TEAM_LANGGRAPH_RECURSION_LIMIT # graph superstep cap. default 50
# --timeout (CLI)                 # wall-clock seconds; 0 disables. default 900

# Tail a backgrounded run's real log (harness temp files get reaped)
#   redirect to a file you own, e.g. scratchpad/s2.log, then grep:
grep -iE "fallback_extraction|spend_guard|current_phase|success" <logfile>
```

See [docs/GUARDRAILS.md](GUARDRAILS.md) → *LangGraph backend operational guardrails* for the
full reference.

---

## Smoke-test eval session — multi-backend compare (2026-06-28, afternoon)

Follow-on from the operational-guardrails work above. Ran the documented smoke-test eval
(`add(a,b)` + one pytest) across all three backends, then triaged CrewAI failures.

```bash
# Compare all backends (parallel; logs in /tmp/eval_<backend>.log)
uv run python -m evals.run_evals --compare --scenario smoke-test --no-judge

# Single backend
uv run python -m evals.run_evals --backend crewai --scenario smoke-test --no-judge
```

Recommended env for eval runs (already set by `evals/run_evals.py`, but useful when invoking
pytest directly):

```bash
export AI_TEAM_SKIP_POST_RUN=1          # skip post-run LLM self-improvement (~5 min)
export MEMORY_MEMORY_ENABLED=false      # skip ChromaDB embed on subprocess exit
export CREWAI_DISABLE_TELEMETRY=1 NO_COLOR=1 TERM=dumb
```

### Observed results (first compare run)

| Backend | Real outcome | Wall time | Notes |
|---|---|---|---|
| **langgraph** | ✅ Pass | ~76s | `calc.py` + `tests/test_calc.py`; pytest clean |
| **claude-agent-sdk** | ✅ Pass | ~107s | ~$0.29; goal alignment 1.00 |
| **crewai** | ❌ False pass | ~351s + hang | Compare watchdog marked PASSED after `project_complete` but eval pytest never finished |

CrewAI solo re-run (before fixes): **5 failed / 4 passed** in ~1200s — `RecursionError`,
missing test file, timeout exceeded.

### Problems discovered

#### 1. Compare watchdog false-positive (crewai)

`evals/run_evals.py --compare` kills the subprocess 90s after seeing `project_complete` in
the log and **sets exit code 0** even when pytest eval assertions never ran. CrewAI looked
green in the summary while the harness would have failed on `test_has_test_file` /
`test_pytest_passes_in_workspace`.

**Fix:** watchdog kill now requires pytest summary text (` passed in `) in the log; otherwise
exit 1 with `FAILED (hang after project_complete)`.

#### 2. CrewAI Rich console recursion + eval hang

CrewAI's `EventListener` / `ConsoleFormatter` recurses in non-TTY subprocesses
(`update_method_status → print → Live.update → …`). T1 (`_disable_crewai_console`) existed
but was insufficient alone because:

- **Planning/dev/testing crews ran with `verbose=True`** when no TUI monitor was attached
  (eval mode). Log showed `planning_crew_created … verbose=False` only after the fix;
  dev/testing previously defaulted to verbose on.
- **`run_ai_team` return path** tried `json.dumps(enriched, default=str)` on the full flow
  state → `RecursionError` on circular LangChain refs, sometimes **after** `project_complete`
  (14+ min hang, then abort).

**Fixes (crewai backend + main flow):**

- `_disable_crewai_console()` strengthened (devnull Rich console + `_is_streaming=True`); re-applied at start of each `CrewAIBackend.run()`.
- `_crew_verbose()` on `AITeamFlow` — default **False** for all crews when no monitor; explicit `--verbose` still works.
- `_flatten_crewai_payload()` — extract JSON-safe state subset; `success` from `current_phase == "complete"`, not hardcoded `True`.
- Kickoff respects pre-set `PROJECT_WORKSPACE_DIR` and nests `/<project_id>/` for eval tmp workspaces.
- Unit test `test_console_formatter_disabled` (T2 regression guard).

#### 3. `--no-judge` ignored

`evals/run_evals.py` sets `EVAL_NO_JUDGE=1`, but eval modules hardcoded `run_judge=True` in
`compute_metrics()`. Judge still ran on langgraph/claude-sdk (extra latency + API cost).

**Fix:** `compute_metrics()` checks `EVAL_NO_JUDGE` before invoking `LLMJudge`.

#### 4. QA hallucinated pytest results (crewai)

QA agent returned structured JSON claiming `6 passed` with **fake** pytest stdout
(`platform linux -- Python 3.8.10`) while **no test file existed on disk**. The test-execution
guardrail accepted JSON counts without verifying `run_pytest` was called.

**Fixes:**

- `run_pytest()` registers a **verified run** in-process (`get_verified_pytest_run()` /
  `clear_verified_pytest_run()`).
- `agent_test_result_matches_verified()` + `looks_like_fabricated_pytest_output()` — guardrail
  rejects agent JSON that doesn't match a real tool invocation (platform/Python mismatch,
  count mismatch, raw_output not from tool).
- `testing_crew.kickoff()` final result = verified tool output **or**
  `run_pytest_discover_workspace()` fallback — **never** trusts parsed agent JSON alone.
- Removed `_extract_test_result_from_pytest_stdout()` (accepted prose-embedded fake stdout).
- Test generation output persisted to disk via `_persist_test_files_from_generation()`.
- Unit tests: `tests/unit/tools/test_pytest_verification.py`.

#### 5. Root-level test file write blocked

Development wrote `test_calc.py` at workspace root; `write_file()` **refused** with
`Refusing to write root-level pytest file`. Only `calc.py` landed; eval failed
`test_has_test_file`. Log line: `development_file_write_failed path=test_calc.py`.

**Fix:** `normalize_pytest_path()` — auto-relocate `test_*.py` at root → `tests/test_*.py`
(used in `write_file`, development phase, QA test persistence). Test updated in
`test_file_tools.py`.

#### 6. Crew memory ignored `MEMORY_MEMORY_ENABLED=false`

Eval sets `MEMORY_MEMORY_ENABLED=false`, but planning/dev crews still logged
`memory=True` and initialized Chroma embedder. Observed **segfault** entering testing crew
(native extension stack through crewai hooks / posthog / chromadb threads).

**Fix:** `crew_memory_enabled()` in `src/ai_team/crews/memory_flag.py`; planning,
development, and testing crews default `memory=None` → read `settings.memory.memory_enabled`.
When eval disables memory, all crews run with `memory=False`, no embedder.

#### 7. Eval workspace resolution

Eval passed tmp workspace root but files lived under `<ws>/<project_id>/`. File checks missed
nested layout.

**Fix:** `eval_result_from_run()` resolves `workspace_dir / project_id` when that subdir
exists. `retry_count` sums CrewAI `retry_counts` dict.

#### 8. Orchestrated pytest scoped to repo cwd (not project workspace)

After skipping agent `test_execution` and running `run_pytest_discover_workspace()` in
`testing_crew.kickoff()`, the fallback used **`Path.cwd().rglob("test_*.py")`**. In eval
subprocesses, cwd is the **ai-team repo root**, not the per-run tmp workspace under
`PROJECT_WORKSPACE_DIR`.

Observed on second re-run (~18:14):

```
pytest_discover_fallback  count=652  test_dir=/Users/.../ai-team/evals
pytest_run_verified       failed=0 passed=0 success=False total=0
testing_complete          quality_gate_passed=False
routing_after_testing     decision=retry_development retry_count=1
```

Pytest ran the **entire eval harness** (~5 min) instead of `tests/test_calc.py` in the
project workspace. `success=False` with `total=0` triggered `retry_development`, which
re-entered development + testing and blew the eval **1200s pytest timeout** (stack traces
show CrewAI Rich `Live` threads and `CrewAISyncHandler_*` still alive).

**Fix:** `run_pytest_discover_workspace()` now:

- Resolves tests from `settings.project.workspace_dir` (not cwd).
- Prefers `workspace/tests/test_*.py`, then root-level `test_*.py` in workspace only.
- Passes explicit `workspace=` into `run_pytest()` so subprocess cwd is the project dir.
- `run_pytest()` uses workspace as cwd and `.` as source path (not `src/ai_team`).

Unit test: `TestDiscoverWorkspace.test_discovers_tests_only_under_workspace` in
`test_pytest_verification.py`.

#### 9. Agent `test_execution` guardrail retry storm (empty LLM)

Even with verified-run guardrails, the agent `test_execution` task retried 3× when QA never
called `run_pytest`, then OpenRouter returned **empty** responses →
`ValueError: Invalid response from LLM call - None or empty` → `handle_testing_error`.
The discover fallback never ran because `crew.kickoff()` raised first.

**Fix (orchestration, not LLM layer):** default `agent_test_execution=False` in
`create_testing_crew()` — only `test_generation` + `code_review` run as agent tasks; pytest
is orchestrated in `kickoff()` with `try/except` so salvage runs even if the crew raises.

#### 10. Stale settings singleton in eval subprocess

`get_settings()` caches a module-level singleton. If anything imported settings before
`MEMORY_MEMORY_ENABLED=false` was applied, crews could still see `memory=True` despite eval
env.

**Fix:** `reload_settings()` at start of `CrewAIBackend.run()`. Second re-run confirmed
`planning_crew_created memory=False`.

### Backend smoke-test status (after fixes)

CrewAI smoke eval **not green yet**. Progress by re-run:

| Re-run | Wall time | Outcome |
|---|---|---|
| First solo (pre-fix) | ~1200s | 5 failed — RecursionError, missing test file, timeout |
| ~17:46 | ~17.5 min | `handle_testing_error` — empty LLM during QA guardrail retries; dev write still blocked |
| ~18:14 (orchestrated pytest) | ~140 min | Dev write ✅, memory=False ✅, orchestrated pytest ❌ (652 eval tests), harness timeout |

Re-run after workspace-scoped pytest fix:

```bash
uv run python -m evals.run_evals --backend crewai --scenario smoke-test --no-judge
# expect: pytest_discover_fallback workspace=<tmp>/.../ count=1 test_dir=tests
#         pytest_run_verified passed>=1 success=True
#         no retry_development, wall time < 900s, 12/12 eval assertions pass
```

Log: `/tmp/eval_crewai_solo.log`

LangGraph and Claude SDK were already green on the first compare run; no regressions expected
from eval-harness changes.

### Re-run result (2026-06-28 ~17:46, ~17.5 min)

Command: `uv run python -m evals.run_evals --backend crewai --scenario smoke-test --no-judge`
Log: `/tmp/eval_crewai_solo.log`

| Check | Result |
|---|---|
| RecursionError / console hang | ✅ None — `verbose=False`, flow progressed |
| Embed skip | ✅ `model_validation_embed_skipped reason=memory_disabled` |
| Planning crew memory | ❌ Log still showed `memory=True` (likely pre-`memory_flag` run; current code returns `False`) |
| Dev test file on disk | ❌ `development_file_write_failed path=test_calc.py` — only `calc.py` written |
| QA hallucination guard | ✅ Guardrail blocked 3×: `run_pytest tool was not called` |
| Final outcome | ❌ `handle_testing_error` — empty OpenRouter LLM responses during guardrail retries |

**Takeaway:** Anti-hallucination guardrail is working (QA can no longer fake pass), but the run still fails because (a) dev never persisted `tests/test_calc.py`, and (b) QA agent never successfully invoked `run_pytest` before LLM returned empty. Re-run with current tree (`normalize_pytest_path` + `crew_memory_enabled` wired) to confirm dev write fix; if QA still skips `run_pytest`, rely on `run_pytest_discover_workspace()` fallback in `testing_crew.kickoff()` or tighten QA tool-forcing.

### Follow-up fix (same session, post re-run)

Root cause of testing crash: agent `test_execution` task retries guardrail 3× without calling
`run_pytest`, then OpenRouter returns empty → `ValueError`. The discover fallback in
`kickoff()` never ran because `crew.kickoff()` raised first.

**Changes:**

| Fix | Detail |
|---|---|
| Orchestrated pytest | Default: skip agent `test_execution`; run `run_pytest_discover_workspace()` after crew (even if crew raises) |
| Dev test persistence | `_persist_test_files_from_code_files()` at testing kickoff start (relocates `test_calc.py` → `tests/`) |
| Resilient kickoff | `try/except` around `crew.kickoff()` — salvage pytest on LLM empty-response crash |
| Settings reload | `reload_settings()` at `CrewAIBackend.run()` so `MEMORY_MEMORY_ENABLED=false` is not stale |

### Second re-run result (2026-06-28 ~18:14, ~140 min — failed)

Command: same as above. Log: `/tmp/eval_crewai_solo.log`

| Check | Result |
|---|---|
| `memory=False` on planning crew | ✅ `planning_crew_created memory=False` |
| Dev files on disk | ✅ `calc.py` + `tests/test_calc.py` (`normalize_pytest_path` working) |
| Agent test_execution skipped | ✅ No guardrail retry storm / empty LLM crash |
| Orchestrated pytest scope | ❌ `count=652 test_dir=.../evals` — repo cwd, not workspace |
| Pytest outcome | ❌ `success=False total=0` after ~5 min on wrong test tree |
| Flow routing | ❌ `retry_development` → second dev/testing cycle |
| Eval harness | ❌ `Timeout` on `test_completes_successfully` / `test_required_files_exist` (1200s) |

**Root cause of this failure:** problem **#8** above. Fixed locally; not re-verified end-to-end.

### Updated eval / crewai parity notes

| Concern | langgraph | claude-agent-sdk | crewai (after fixes) |
|---|---|---|---|
| Smoke eval pass | ✅ | ✅ | ⏳ re-verify (workspace pytest fix pending) |
| Prose/hallucinated QA pytest | n/a (real pytest in graph) | n/a (reliable tools) | ✅ verified-run guardrail + orchestrated pytest |
| Orchestrated pytest workspace scope | n/a | n/a | ✅ fixed (was collecting `evals/**`) |
| Compare watchdog honest | ✅ | ✅ | ✅ no longer fakes PASS |
| Crew memory off in eval | ✅ | n/a | ✅ `memory=False` confirmed on re-run |
| `--no-judge` honored | ✅ | ✅ | ✅ |

### Open items (eval / crewai)

1. **Re-run crewai smoke eval to green** after workspace-scoped `run_pytest_discover_workspace()`.
2. **Compare JSON report** — `--compare` does not write `evals/results/comparison_*.json`
   (only `evals/test_backend_comparison.py` does). Wire summary JSON into compare mode if
   needed for dashboards.
3. **Empty LLM responses** — mitigated by skipping agent `test_execution`; still possible on
   `test_generation` / `code_review` under load.
4. **Development crew Dockerfile tool noise** — smoke run hit `Dockerfile generator` pydantic
   validation errors (missing `port`); wasted dev iterations on a calculator. Consider
   skipping devops task for `prototype` profile on trivial scenarios.
5. **Retry loop on false pytest failure** — if orchestrated pytest returns `success=False`,
   flow still enters `retry_development` (up to 3×). Consider short-circuit when tests exist
   on disk but pytest subprocess mis-scoped, or cap smoke profile to 0 dev retries.
6. **Segfault / thread hang on timeout** — timed-out eval showed Rich `Live` + CrewAI handler
   threads alive; may need explicit crew shutdown or shorter per-phase timeout for smoke.
7. **Segfault root cause (earlier runs)** — likely Chroma/native + crewai event bus; mitigated
   by `memory=False`; if segfault persists, run under `PYTHONFAULTHANDLER=1`.

### Files touched (eval / crewai session)

| Area | Files |
|---|---|
| CrewAI backend | `src/ai_team/backends/crewai_backend/backend.py` |
| Flow verbose + writes | `src/ai_team/flows/main_flow.py` |
| Pytest verification | `src/ai_team/tools/test_tools.py`, `src/ai_team/tasks/testing_tasks.py`, `src/ai_team/crews/testing_crew.py` |
| Test path normalize | `src/ai_team/tools/file_tools.py` |
| Crew memory flag | `src/ai_team/crews/memory_flag.py`, `planning_crew.py`, `development_crew.py`, `testing_crew.py` |
| Testing crew orchestration | `src/ai_team/crews/testing_crew.py` (orchestrated pytest, dev test persist, resilient kickoff) |
| Eval harness | `evals/metrics.py`, `evals/fixtures.py`, `evals/run_evals.py` |
| Tests | `tests/unit/tools/test_pytest_verification.py`, `tests/unit/tools/test_file_tools.py`, `tests/unit/backends/test_crewai_backend.py`, `tests/unit/crews/test_testing_crew.py` |

---

## Follow-up: smoke-test re-compare + CrewAI go/no-go (2026-06-30)

Re-ran the smoke-test eval across all three backends to answer "do we keep CrewAI?".

```bash
# per-backend pytest, parallel, EVAL_NO_JUDGE=1, MEMORY_MEMORY_ENABLED=false, --timeout=1100
uv run pytest -q --timeout=1100 evals/backends/test_langgraph_eval.py
uv run pytest -q --timeout=1100 evals/backends/test_claude_sdk_eval.py
uv run pytest -q --timeout=1100 evals/backends/test_crewai_eval.py
```

| Backend | Result | Wall time |
|---|---|---|
| langgraph | ✅ 12 passed, 4 skipped | 60s |
| claude-agent-sdk | ✅ 10 passed, 2 skipped | 200s |
| crewai | ❌ hung in first test; **ignored its own `--timeout=1100`** (Rich/event-bus threads swallow the SIGALRM — open item #6 reproduced); force-killed `kill -9` at ~12 min | — |

Note: avoid `evals.run_evals --compare` for tracking from an automated session — it spawns detached subprocesses and returns immediately (no final summary). Run the three `pytest` files directly under one `wait`.

**Decision (recorded in [journey.md](journey.md) Jun 30 + [substack post](posts/substack_post.md)):** CrewAI is **demoted from a supported production path to a comparison-only datapoint**. It is *not* a model problem — same deepseek-v3 passes on LangGraph in 60s — it's that CrewAI's runtime fights headless, file-handoff orchestration (Rich console recursion, flow-state `RecursionError`, embedder segfault, post-complete re-trigger, un-killable timeout). Two sessions of CrewAI-specific defenses still didn't get the smoke test green; bad effort-to-reliability ratio. Keep it in the eval matrix *because* the failure is a real finding; stop whack-a-mole.

**Bounded last attempt before declaring it known-red** (do these, then stop if still red):
1. `subprocess`-level hard kill wrapper around the CrewAI run so the wall-clock timeout actually reaps it (don't rely on in-process SIGALRM — open item #6).
2. "tests exist on disk → don't `retry_development`" short-circuit (open item #5) — caps the smoke profile at 0 dev retries.
