---
description: "Design REST APIs following OpenAPI-friendly patterns"
tools: ["Read", "Write", "Glob", "Grep"]
---

# API Design Skill

When designing HTTP APIs:

1. Prefer resource-oriented URLs and consistent status codes
2. Document request/response shapes (JSON), pagination, and error format
3. Consider idempotency for mutating operations where appropriate
