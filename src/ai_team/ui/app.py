"""Gradio app for ai-team — placeholder until Phase 6 UI is implemented."""

import gradio as gr

with gr.Blocks(title="AI Team") as demo:
    gr.Markdown("# AI Team")
    gr.Markdown("> Gradio UI will be implemented in Phase 6. Use `ai-team` CLI in the meantime.")
