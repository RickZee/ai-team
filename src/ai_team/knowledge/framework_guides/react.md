# React guide

- Prefer functional components with hooks; colocate state near usage.
- Split presentational and container concerns when components grow large.
- Use keys on lists; avoid inline object/array literals in JSX props when they cause re-renders.
- Type props with TypeScript or PropTypes for maintainability.
