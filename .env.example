# Agentic AI Team — environment template
# Copy to .env and fill in values as needed.
# Do not commit .env (it is gitignored).

# Ollama (required for local agents)
OLLAMA_BASE_URL=http://localhost:11434

# Optional: OpenAI (for Memory embeddings/analysis if not using Ollama embedder)
# OPENAI_API_KEY=sk-...

# Optional: GitHub (only if using GitHub tool with write actions)
# GITHUB_TOKEN=ghp_...

# Optional: AWS (only if using boto3 tool; use read-only IAM)
# AWS_ACCESS_KEY_ID=...
# AWS_SECRET_ACCESS_KEY=...
# AWS_DEFAULT_REGION=us-east-1
