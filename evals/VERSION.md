# evals package version

`0.1.0` versions the **eval harness CLI and check registry**, not the failure
taxonomy. The taxonomy is versioned separately in
`evals/taxonomy/failure_modes.yaml` (`1.2.0`, FM-001…017).
